extends layouts/main
include mixins/videoBlock   
    
block content
    //- 클래스 명이 videos 라는 div 태그를 만든다.
    .videos 
        //- videoController 에서 videos 라는 애들 던져 줬는데 iterable 한 녀석이다.
        each item in videos 
            //- h1= item.title
            //- p= item.description
            //- mixin 을 사용하는 방법 + 를 하면 된다. videoBlock( ) 안에 객체를 만들기 위해서 { }를 하고 이 안에 내용을 적는다.
            +videoBlock({
                id: item.id,
                videoFile:  item.videoFile,
                title:  item.title,
                views:  item.views
            })